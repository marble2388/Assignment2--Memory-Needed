@model IEnumerable<Assignment1.Models.ParentTable>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.otherthing1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.otherthing2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.otherthing3)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.otherthing1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.otherthing2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.otherthing3)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
